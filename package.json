{
  "name": "json-server",
  "version": "1.0.0",
  "description": "Deploy JSON Server to Render",
  "main": "api/server.js",
  "scripts": {
    "start": "node api/server.js"
  },
  "devDependencies": {
    "json-server":"^0.17.4",
    "json-server-auth": "^2.1.0"
  }
}
